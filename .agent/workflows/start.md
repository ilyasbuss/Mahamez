---
description: Start the Mahamez development server (frontend & backend)
---

To start the server, run the following command:

// turbo
1. Run the startup script:
```powershell
cmd /c start_server.bat
```

Wait for the "Server running on port 8000" and "VITE ... ready" messages.
